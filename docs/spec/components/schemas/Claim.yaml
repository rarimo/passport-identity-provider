allOf:
  - $ref: '#/components/schemas/ClaimKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - claim_id
          - issuer_did
        properties:
          claim_id:
            type: string
          issuer_did:
            type: string